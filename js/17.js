/* //! console.time() і console.timeEnd() (Вимірювання часу виконання)
TODO: Методи для вимірювання часу виконання коду в мілісекундах між початком і кінцем.
 * - `console.time(label)` запускає таймер із унікальною міткою (label), яку задаєш як рядок.
 * - `console.timeEnd(label)` зупиняє таймер із відповідною міткою і виводить час у консоль.
 * - Аргумент `label` має бути однаковим для пари `time` і `timeEnd`, щоб вимірювання працювало.
 * - Час виводиться в консоль у форматі "label: X.XXX ms" (наприклад, "loop: 2.345 ms").
 * - Корисно для тестування швидкості виконання коду чи порівняння різних підходів.

TODO: Синтаксис:
console.time('мітка'); // Початок таймера
  Код, який хочемо виміряти
console.timeEnd('мітка'); // Кінець таймера, виводить час

TODO: Приклад 1. Вимірювання часу простого циклу
console.time('simple-loop'); // Починаємо таймер
for (let i = 0; i < 1000000; i++) { // Цикл із мільйоном ітерацій
    Порожній блок
}
console.timeEnd('simple-loop'); // Зупиняємо таймер
  Виводить: 'simple-loop: X.XXX ms' (час залежить від системи, наприклад, 2.345 ms)

TODO: Приклад 2. Вимірювання часу обчислення суми масиву
console.time('array-sum'); // Починаємо таймер
const arr = [1, 2, 3, 4, 5]; // Створюємо масив
let sum = 0; // Змінна для суми
for (const num of arr) { // Перебираємо масив
  sum += num; // Додаємо кожне число
}
console.timeEnd('array-sum'); // Зупиняємо таймер
  Виводить: 'array-sum: X.XXX ms' (наприклад, 0.015 ms)

TODO: Приклад 3. Вимірювання часу порожнього блоку
console.time('empty'); // Починаємо таймер
  Нічого не робимо
console.timeEnd('empty'); // Зупиняємо таймер
  Виводить: 'empty: X.XXX ms' (дуже малий час, наприклад, 0.001 ms)
*/
