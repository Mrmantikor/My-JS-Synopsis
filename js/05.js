//#region //? Розгалуження
/* //! if (Розгалуження - Умовний оператор)
TODO: Виконує блок коду, якщо умова істинна, без альтернативних гілок else if чи else, включаючи запуск циклів за умовою.
 * - Умовний оператор, який перевіряє умову і виконує блок коду, якщо результат умови — true.
 * - Умова — вираз, який приводиться до булевого типу (true або false).
 * - Truthy значення: ненульові числа, непорожні рядки, об’єкти, масиви тощо.
 * - Falsy значення: 0, '', null, undefined, NaN, false.
 * - Без else: якщо умова false, код у блоці просто пропускається.
 * - Корисно для простих перевірок чи запуску дій за умовою.

TODO: Синтаксис:
if (умова) {
  Код, який виконується, якщо умова true
}

TODO: Приклад 1. Перевірка числа з виведенням
const number = 10;
if (number > 0) {
  console.log(`Число ${number} є додатним`);
}
  Виводить: 'Число 10 є додатним'

TODO: Приклад 2. Перевірка рядка на порожність
const text = '';
if (text) {
  console.log('Рядок не порожній');
}
  Виводить: нічого (умова false, бо '' — falsy)
*/
/* //! if...else (Розгалуження - Умовний оператор із альтернативою)
TODO: Виконує один із двох блоків коду залежно від того, чи виконується умова: if для true, else для false.
 * - Умовний оператор, який перевіряє умову і виконує блок if, якщо умова істинна (true), або блок else, якщо хибна (false).
 * - Умова — вираз, який приводиться до булевого типу (true/false).
 * - Truthy значення: ненульові числа, непорожні рядки, об’єкти тощо.
 * - Falsy значення: 0, '', null, undefined, NaN, false.
 * - Корисно для двостороннього вибору: одна дія для "так", інша для "ні".

TODO: Синтаксис:
if (умова) {
    Код, якщо умова true
} else {
    Код, якщо умова false
}

TODO: Приклад 1. Перевірка віку
const age = 16;
if (age >= 18) {
  console.log('Ви повнолітній');
} else {
  console.log('Ви неповнолітній');
}
  Виводить: 'Ви неповнолітній'

TODO: Приклад 2. Перевірка парності числа
const number = 7;
if (number % 2 === 0) {
  console.log('Число парне');
} else {
  console.log('Число непарне');
}
  Виводить: 'Число непарне'
*/
/* //! if...else if...else (Розгалуження - Умовний оператор із кількома гілками)
TODO: Виконує один із кількох блоків коду залежно від виконання умов, із фінальною альтернативою else.
 * - Умовний оператор, який перевіряє умови по черзі і виконує перший блок із істинною умовою (true).
 * - Якщо жодна умова не виконана, виконується блок else (якщо є).
 * - Умова — вираз, який приводиться до булевого типу (true/false).
 * - Truthy значення: ненульові числа, непорожні рядки, об’єкти тощо.
 * - Falsy значення: 0, '', null, undefined, NaN, false.
 * - Корисно для вибору між кількома варіантами залежно від умови.

TODO: Синтаксис:
if (умова1) {
    Код, якщо умова1 true
} else if (умова2) {
    Код, якщо умова2 true
} else {
    Код, якщо жодна умова не true
}

TODO: Приклад 1. Перевірка температури
const temperature = 25;
if (temperature > 30) {
  console.log('Дуже жарко');
} else if (temperature > 20) {
  console.log('Тепло');
} else {
  console.log('Прохолодно');
}
  Виводить: 'Тепло'

TODO: Приклад 2. Оцінка за балом
const score = 75;
if (score >= 90) {
  console.log('Відмінно');
} else if (score >= 80) {
  console.log('Дуже добре');
} else if (score >= 70) {
  console.log('Добре');
} else {
  console.log('Задовільно');
}
  Виводить: 'Добре'

TODO: Приклад 3. Перевірка числа
const number = -5;
if (number > 0) {
  console.log('Число позитивне');
} else if (number === 0) {
  console.log('Число дорівнює нулю');
} else {
  console.log('Число негативне');
}
  Виводить: 'Число негативне'
*/
/* //! Ternary Operator (Тернарний оператор)
TODO: Компактна форма умовного оператора, яка повертає одне з двох значень залежно від умови.
 * - Умова перевіряється і приводиться до булевого типу (true або false).
 * - Якщо умова true, повертається перше значення (перед двокрапкою), якщо false — друге (після двокрапки).
 * - Truthy значення: ненульові числа (наприклад, 5), непорожні рядки ('Hello'), об’єкти тощо.
 * - Falsy значення: 0, порожній рядок (''), null, undefined, NaN, false.
 * - Корисно для коротких умовних виразів, коли потрібно швидко вибрати одне з двох значень.

TODO: Синтаксис:
умова ? значення_якщо_true : значення_якщо_false

TODO: Приклад 1. Перевірка повноліття
const age = 20;
const status = age >= 18 ? 'Повнолітній' : 'Неповнолітній';
console.log(status);
  Виводить: 'Повнолітній'

TODO: Приклад 2. Визначення погоди за температурою
const temperature = -5;
const weather = temperature > 0 ? 'Тепло' : 'Холодно';
console.log(weather);
  Виводить: 'Холодно'

TODO: Приклад 3. Перевірка довжини рядка
const text = 'Hello';
const lengthCheck = text.length > 3 ? 'Довгий рядок' : 'Короткий рядок';
console.log(lengthCheck);
  Виводить: 'Довгий рядок'

TODO: Приклад 4. Визначення знаку числа
const number = 0;
const sign = number > 0 ? 'Позитивне' : 'Негативне або нуль';
console.log(sign);
  Виводить: 'Негативне або нуль'
*/
/* //! switch (Перемикач - Умовний оператор із випадками)
TODO: Виконує один із кількох блоків коду залежно від значення виразу, порівнюючи його з набором випадків.
 * - Умовний оператор, який порівнює значення виразу з випадками (case) і виконує відповідний блок.
 * - Використовує суворе порівняння (===) між виразом і значенням кожного case.
 * - Блок default виконується, якщо жоден case не збігається (необов’язковий).
 * - Без break код "провалюється" (fall-through) до наступних case.
 * - break зупиняє виконання switch після збігу.
 * - Корисно для чіткого вибору між кількома фіксованими значеннями чи умовами.

TODO: Синтаксис:
switch (вираз) {
  case значення1:
      Код, якщо вираз === значення1
    break;
  case значення2:
      Код, якщо вираз === значення2
    break;
  default:
      Код, якщо жоден case не збігається
}

TODO: Приклад 1. Перевірка температури
const temperature = 25;
switch (temperature) {
  case 30:
    console.log('Жарко');
    break;
  case 25:
    console.log('Тепло');
    break;
  case 15:
    console.log('Прохолодно');
    break;
  default:
    console.log('Невідома температура');
}
  Виводить: 'Тепло'

TODO: Приклад 2. Оцінка за балом
const score = 85;
switch (true) {
  case score >= 90:
    console.log('Відмінно');
    break;
  case score >= 80:
    console.log('Добре');
    break;
  case score >= 70:
    console.log('Задовільно');
    break;
  default:
    console.log('Низький бал');
}
  Виводить: 'Добре'
*/
//#endregion

/* //! switch (Вибір напою)
TODO: Запитати у користувача напій і вивести ціну залежно від вибору.
 * - Використати prompt для отримання напою від користувача.
 * - Перевірити вибір за допомогою switch (кава, чай, сік).
 * - Вивести ціну (1, 2, 3) або повідомлення, якщо напій невідомий.
 * - Нічого не повертає, лише виводить результат.
 * - Корисно для роботи з умовними конструкціями та введенням даних.

TODO: Task
function chooseDrink() {
}

chooseDrink(); // Виводить: ціну (1, 2, 3) або 0 залежно від введення

TODO: Answer - Var 1 (з switch)
function chooseDrink() {
  let drink = prompt('Що бажаєте?')?.toLowerCase();
  let price = 0;
  switch (drink) {
    case 'кава':
      price = 1;
      break;
    case 'чай':
      price = 2;
      break;
    case 'сік':
      price = 3;
      break;
    default:
      console.log('Що це?');
  }
  console.log(price);
}

chooseDrink(); // Виводить: ціну (1, 2, 3) або 0 залежно від введення
*/
